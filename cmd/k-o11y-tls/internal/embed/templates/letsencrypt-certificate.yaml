apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: otel-collector-cert
  namespace: {{.Namespace}}
spec:
  secretName: {{.SecretName}}
  duration: 2160h
  renewBefore: 720h
  dnsNames:
    - {{.Domain}}
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
